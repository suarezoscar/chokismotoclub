declare module 'circletype' {
  // You can add specific type definitions here if needed
  // For now, you can use `any` if you're not sure about the types
  const CircleType: any;
  export default CircleType;
}
